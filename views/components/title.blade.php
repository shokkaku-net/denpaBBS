<div class="boardTitle">
    @if (!empty($logo))
        <img class="logo" src="$logo" \>
    @endif
    <h1 class="title">
        {!! $title !!}
    </h1>
    <h5 class="subtitle">
        {!! $subtitle !!}
    </h5>
</div>